from pathlib import Path
import runpy
import os
import site

BASE = Path(__file__).resolve().parent
site.addsitedir(str(BASE / "vendor"))

GAME_DIR = BASE
MAIN = GAME_DIR / "game" / "main.py"

os.chdir(GAME_DIR)
runpy.run_path(str(MAIN), run_name="__main__")
